steps:
  mtaBuild:
    buildTarget: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      apiEndpoint: 'https://api.cf.eu10.hana.ondemand.com'
      org: '4683088dtrial'
      space: 'dev'
      credentialsId: 'CF-jenkins-credentials-id'
  sonarExecuteScan:
    serverUrl: 'https://sqenterpriseqa.pgcloud.com/sonarqube'
  tmsUpload:
    credentialsId: tms_secret_id
    nodeName: uat
    mtaPath: com.sap.piper.node.hello.world.mtar

stages:
  Release:
    tmsUpload: true
