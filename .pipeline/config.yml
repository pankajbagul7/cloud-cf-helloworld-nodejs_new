general:
  steps:
  mtaBuild:
    buildTarget: 'CF'
    verbose: true
    script: this
      cloudFoundryDeploy:
     deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
        org: 'trial'
        space: 'dev'
        credentialsId: 'CF-jenkins-credentials-id'
