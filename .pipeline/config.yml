general:
steps:
  mtaBuild:
    buildTarget: 'CF'
    cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    verbose : true
    cloudFoundry:
        org: 'trial'
        space: 'dev'
        credentialsId: 'CF-jenkins-credentials-id'
