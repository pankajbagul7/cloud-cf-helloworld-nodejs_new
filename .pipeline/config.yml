general:
steps:
  mtaBuild:
    buildTarget: 'CF'
    script: this
    cloudFoundryDeploy:
    verbose: true
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
        org: 'trial'
        space: 'dev'
        credentialsId: 'CF-jenkins-credentials-id'
