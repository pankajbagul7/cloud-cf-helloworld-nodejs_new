general:
steps:
  mtaBuild:
    buildTarget: 'CF'
      cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'c85591detrial'
      space: 'dev'
      credentialsId: 'CF-jenkins-credentials-id'
